# Enable internal SPI usage for realm REST API extensions
# This suppresses the warning and allows the provider to load in Keycloak 26+
spi-realm-restapi-extension-enabled=true

# ABCA LOTL refresher configuration (can also be provided via system properties or env vars):
# abca.lotl.url=https://example.com/lotl.pem
# abca.lotl.refresh-interval-seconds=300
# abca.lotl.http-timeout-ms=15000
